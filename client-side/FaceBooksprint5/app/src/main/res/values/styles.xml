<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:colorPrimary">@color/titlebackgoundColor</item>
        <item name="android:title">@color/titlebackgroundColor1</item>
        <item name="android:titleTextColor">@color/titletextcolor</item>
    </style>
    <style name="AppTheme1" parent="android:Theme.Holo.Light.DarkActionBar">
        <item name="android:colorPrimary">@color/titlebackgroundColor1</item>
    </style>

</resources>
